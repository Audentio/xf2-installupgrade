{
    "template": "addon_list_macros",
    "description": "JSON hash changes",
    "execution_order": 10,
    "enabled": true,
    "action": "str_replace",
    "find": "$addOn.hasPendingChanges()",
    "replace": "$0 || $addOn.getInstalledAddOn().isInstallUpgradeJsonHashChanged()"
}